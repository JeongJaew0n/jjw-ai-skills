# jjw-ai-skills

직접 만든 Claude Code 스킬 모음. 스킬 작성 규칙은 `AGENTS.md` 에 있고, 스킬을 만들거나
고치기 전에 그 파일을 먼저 읽는다.

## 구조

```
skills/<skill-name>/
├── SKILL.md          스킬 1개 = 디렉터리 1개 (디렉터리명 = 스킬명)
└── bin/              스킬이 쓰는 스크립트 (있는 경우)
```

스킬을 새로 만들거나 고치면 `README.md` 의 스킬 목록 표도 함께 갱신한다.

## 프로젝트 규칙 (2026-09-12 확정)

### git

- **author**: `HeLLo2 <popt0@naver.com>` — `--local` 로 고정돼 있지 않고 global 설정을
  물려받는다. global 을 바꾸면 이 저장소의 author 도 조용히 따라 바뀐다.
- **커밋·푸시: 전자동.** 작업이 끝나면 사용자에게 묻지 않고 커밋하고 푸시한다.
  사용자가 2026-09-12 에 명시적으로 지시한 것이다 ("묻지말고 main 에 commit push하도록 해").
- **브랜치: main 고정.** 브랜치를 새로 만들지 않고 main 에 직접 커밋·푸시한다.

푸시가 거부되면(원격에 다른 세션의 커밋이 있는 경우) `git pull --rebase` 로 재배치한 뒤
다시 푸시한다. 이 저장소는 여러 세션이 동시에 건드리므로 자주 일어난다.
`--force` 는 쓰지 않는다.

### docs

- 여러 단계짜리 작업은 코드를 건드리기 전에 `docs/plans/<slug>/` 에 계획을 먼저 쓴다.
- 원인 찾는 데 시간이 걸린 오류는 `docs/troubleshootings/` 에 남긴다. 원인이
  라이브러리·런타임·OS 에 있으면 `reusable/`, 이 저장소의 코드·설정에 있으면
  `project-specific/`.
