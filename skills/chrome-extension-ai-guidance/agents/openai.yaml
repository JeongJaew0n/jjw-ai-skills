# bin/install.sh 가 SKILL.md frontmatter 에서 생성한다. 직접 고치지 말 것.
interface:
  display_name: "chrome-extension-ai-guidance"
  default_prompt: "Use $chrome-extension-ai-guidance."

policy:
  allow_implicit_invocation: false
