# bin/install.sh 가 SKILL.md frontmatter 에서 생성한다. 직접 고치지 말 것.
interface:
  display_name: "obsidian-plugin-local-deployment"
  default_prompt: "Use $obsidian-plugin-local-deployment."

policy:
  allow_implicit_invocation: false
