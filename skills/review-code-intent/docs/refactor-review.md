# 리팩토링 검토 — review-code-intent

> 검토일: 2026-09-01 / 기준 커밋: `fdecefc` / 대상: `SKILL.md` (391줄), `templates/` 3개 (199줄)

근거 등급은 이 스킬 자신의 규약을 따른다. `실행 확인` / `코드 확정` / `미측정`.

## 요약

| 심각도 | 개수 |
|---|---|
| 높음 | 1 |
| 중간 | 3 |
| 낮음 | 2 |

저장소에서 가장 큰 스킬이고 규율도 가장 촘촘하다(근거 등급, 자체 점검, 정정 이력). 문제는 **자기 규율을 자기 frontmatter 와 참조에는 적용하지 않은 것**이다. 존재를 확인하지 않은 스킬 두 개를 가리키고 있는데, 이건 이 스킬이 `# 리뷰 원칙` 4번에서 금지한 바로 그 행위다.

---

## [높음] R-1. `user_invocable` 은 인식되지 않는 키다

> **해소** — 2026-09-01. `user-invocable` (하이픈)으로 고쳤다.
> `disable-model-invocation: true` 도 추가했다 (B안 채택, [결정 기록](#보류--disable-model-invocation)).

**위치** — `SKILL.md:4`

```yaml
user_invocable: true
```

**근거** — `코드 확정`. Claude Code 2.1.251 바이너리에서:

| 표기 | 등장 | 정체 |
|---|---|---|
| `user-invocable` (하이픈) | 31회 | frontmatter 키 목록에 존재 (offset 67001012) |
| `user_invocable` (언더스코어) | 2회 | 둘 다 내부 에러 코드 문자열 `cmd_not_user_invocable` |

런타임 검사는 `if (r.userInvocable === false)` 형태다(offset 170132049). 즉 파싱된 필드명은 `userInvocable` 이고, 그것을 채우는 frontmatter 키는 하이픈 표기다.

**영향은 현재로선 무해하다.** 검사가 `=== false` 이므로 값이 없으면 기본 허용이고, `/review-code-intent` 는 지금도 동작한다. 문제는 두 가지다.

1. 선언이 아무 일도 하지 않는데 하는 것처럼 보인다.
2. 나중에 `false` 로 바꿔 모델 전용 스킬로 만들려 하면 **조용히 실패한다.**

**제안** — 하이픈으로 고치고, 저장소 정책을 강제하는 키를 함께 넣는다.

```yaml
user-invocable: true
disable-model-invocation: true
```

`disable-model-invocation` 은 `AGENTS.md` 의 "명시적 호출 시에만 사용" 정책을 산문이 아니라 하네스 차원에서 강제한다. 안내 문구가 정확히 그 의미다 — `"reserved for explicit user invocation"`.

같은 오타가 `hud/skills/setup/SKILL.md` 에도 있다.

---

## [중간] R-2. 존재하지 않는 스킬 두 개를 가리킨다

**위치** — `SKILL.md:387-391` (`# 관련 skill` 표)

```markdown
| 받은 리뷰 댓글에 응대 | 레포의 `reply-pr-review` (있으면) |
| 프로젝트 컨벤션 집중 점검 | 레포의 `convention-check` 계열 (있으면) |
```

**근거** — `실행 확인`.

| 스킬 | 이 저장소 | `~/.claude/skills` |
|---|---|---|
| `ai-pr-line-comment` | 없음 | **있음** |
| `reply-pr-review` | 없음 | 없음 |
| `convention-check` | 없음 | 없음 |

`(있으면)` 이라는 방어 문구가 붙어 있어 치명적이진 않다. 하지만 이 스킬은 `# 리뷰 원칙 4. 추측과 사실을 구분한다` 와 `## 근거 등급을 반드시 구분한다` 를 핵심 규율로 삼는다. **확인하지 않은 것을 표에 올려둔 것 자체가 그 규율의 위반**이다. 표는 산문보다 사실처럼 읽힌다.

**제안** — 확인된 것만 남기고, 나머지는 성격을 바꿔 적는다.

```markdown
| 상황 | skill |
|---|---|
| 리뷰 결과를 PR 인라인 코멘트로 등록 | `ai-pr-line-comment` (이 저장소 밖 — `~/.claude/skills/`) |

작업 중인 레포에 리뷰 응대·컨벤션 점검 스킬이 있으면 함께 쓴다. 이 저장소는 그런 스킬을
제공하지 않는다.
```

---

## [중간] R-3. `ai-pr-line-comment` 는 이 저장소 밖 의존이다

**위치** — `SKILL.md:389`

표가 `ai-pr-line-comment` 를 조건 없이 가리키지만, 그 스킬은 `~/.claude/skills/` 에만 있고 이 저장소에는 없다. 즉 **이 저장소를 클론한 다른 머신에서는 존재하지 않는다.**

`hud/README.md` 는 이런 종류의 전제를 `## 요구사항` 에 명시하는데, 이 스킬에는 대응하는 절이 없다.

**근거** — `실행 확인`. 위 표와 동일.

**제안** — R-2 의 수정에 `(이 저장소 밖)` 표기를 포함시키면 함께 해소된다. 또는 그 스킬을 이 저장소로 들여온다(별도 판단 필요).

---

## [중간] R-4. `ai-interview-tech` 의 결정 스펙을 의도 항목 출처로 쓰지 못한다

**위치** — `# 3. Intent Implementation Review` 의 `## 의도 항목 확정`

이 스킬은 의도 항목에 출처 등급을 요구한다.

| 등급 | 판정에 쓸 수 있는가 |
|---|---|
| 명시 | 그대로 쓴다 |
| 확인 | 그대로 쓴다 |
| 추론 | **미충족으로 단정하지 않는다** |

그리고 그 이유를 `"내가 코드에서 만들어낸 요구사항으로 그 코드를 채점하는 셈"` 이라 설명한다. 정확한 경계다.

그런데 같은 저장소의 `ai-interview-tech` 가 **구현 전에 사람과 확정한 결정 스펙**을 만든다. 그것이야말로 출처 등급 `명시`/`확인` 의 이상적 공급원인데, 이 스킬은 그 존재를 모른다. 결과적으로 인터뷰를 거친 PR 도 의도 항목을 코드에서 역산하게 된다.

**근거** — `코드 확정`. 양쪽 파일 대조. 두 스킬 어디에도 상대 이름이 없다.

**제안** — `## 의도 항목 확정` 에 항목 하나를 추가한다.

```markdown
## 인터뷰 산출물이 있으면 먼저 찾는다

`docs/plans/<slug>/` 또는 `docs/decisions/` 에 `ai-interview-tech` 가 만든 결정 스펙이
있으면 그것을 의도 항목의 1차 출처로 쓴다. 출처 등급은 `명시` 다. 코드에서 역산하지 않는다.
```

`ai-interview-tech` 쪽에도 대응하는 한 줄이 필요하다(그쪽 검토 문서 I-3).

---

## [낮음] R-5. `"6번까지"` 문구가 재번호 이후 모호해졌다

**위치** — `SKILL.md:60`

```
이 순서를 지킨다. 특히 **6번까지 끝나기 전에 Tech Review를 쓰기 시작하지 않는다.**
```

Intent Implementation Review 축이 추가되며 수행 순서가 9단계에서 11단계로 늘었다. 6번(`intent-review.md` 작성)은 여전히 맞지만, 새로 낀 7번이 **의도 항목 목록 동결**이라 이쪽이 더 중요한 경계다. 지금 문장은 그 사실을 가리지 않는다.

**근거** — `코드 확정`. 60행과 62-72행 대조.

**제안** — 경계를 둘 다 명시한다.

```
이 순서를 지킨다. **6번(intent-review) 과 7번(의도 항목 동결)이 끝나기 전에 Tech Review를
쓰기 시작하지 않는다.** 구현 결함을 먼저 보면 의도 파악이 그쪽으로 끌려가고, 요구사항
목록을 구현에 맞춰 역산하게 된다.
```

---

## [낮음] R-6. 391줄은 한 파일로 크다

**위치** — 파일 전체

| 구간 | 대략 |
|---|---|
| 공통(대상 저장소·수행 순서·산출물·폴더 규칙) | ~120줄 |
| 3개 축 정의 | ~135줄 |
| 리뷰 원칙 8개 | ~90줄 |
| 자체 점검 + 관련 skill | ~30줄 |

`ai-interview-tech` 는 결정 축 카탈로그를 `references/` 로 빼서 "판단이 안 설 때만" 읽게 한다. 이 스킬에서 같은 처리가 가능한 후보는 **`# 리뷰 원칙` 8개**(90줄)다. 매 리뷰마다 필요한 절차가 아니라 판단이 흔들릴 때 참조하는 성격이기 때문이다.

**다만 지금 당장 옮기는 것은 추천하지 않는다.** 원칙 3(고립해서 보지 않기)과 7(이 PR이 만든 문제인지)은 실제로 리뷰 중 계속 적용돼야 하는 규칙이라, 분리하면 안 읽힐 위험이 있다. 분리 이득(컨텍스트 절약)과 손실(규율 약화)이 팽팽하다.

**근거** — `미측정`. 분리 후 리뷰 품질 변화를 측정하지 않았다.

**제안** — 보류. 스킬이 더 커지면 그때 `references/review-principles.md` 로 분리하되, 원칙 3·7 은 본문에 남긴다.

---

## 손대지 않기를 권하는 것

- **근거 등급 3단계** (`실행 확인` / `코드 확정` / `미측정`) — 이 저장소 전체가 빌려 쓸 만한 규율이다. 이 검토 문서들도 그것을 쓰고 있다.
- **`# 완료 전 자체 점검` 13개 항목** — 산출물이 3개로 늘어난 지금 더 필요해졌다.
- **템플릿의 `## 정정 이력` 표** — "조용히 고치지 않는다"는 이 스킬의 가장 좋은 규칙 중 하나다.
- **Tech / Intent Implementation 경계 표와 4케이스 예시** — 세 문서 체계에서 가장 헷갈리는 지점을 정확히 겨냥한다.

---

## 보류 — disable-model-invocation

> **결정됨 (B안)** — 2026-09-01. `disable-model-invocation: true` 를 넣기로 했다. 아래는 그 결정에 이르기까지의 판단 근거이며, 기록으로 남긴다.

이 키는 자동 선택만 막는 게 아니라 **Skill 도구 호출을 전면 차단**한다. 그래서 정책 변경을 수반했다.

이 키는 자동 선택만 막는 게 아니라 **Skill 도구 호출을 전면 차단**한다.

```
Ask the user to run /<name> themselves
— it cannot be invoked via the <tool> tool in this session,
  by the coordinator or by workers
<X> cannot be used with <Y> tool due to disable-model-invocation.
```

`근거` — `코드 확정`. Claude Code 2.1.251 바이너리 offset 71210541.

그런데 `AGENTS.md` 는 이름 직접 언급을 허용한다.

> 사용자가 스킬 이름을 **직접 언급하거나** `/skill-name`, `$skill-name` 형태로 호출한 경우에만 해당 스킬을 사용한다.

이름 직접 언급(`"review-code-intent 로 봐줘"`)은 Skill 도구를 거쳐야 하므로, 이 키를 넣으면 **정책이 허용한 경로가 죽는다.** 즉 이 키는 저장소 정책보다 엄격하다.

선택은 둘이다.

| 안 | 내용 | 대가 |
|---|---|---|
| A (기각) | 넣지 않는다. 정책은 `description` 산문으로 유지 | 강제력이 프롬프트 수준에 머문다 |
| **B (채택)** | 넣는다. 슬래시 전용으로 좁힌다 | 이름 언급 경로 상실. `AGENTS.md` 와 4개 `description` 을 함께 고쳤다 |

채택 근거 — 이 정책이 생긴 원인이 "산문으로 부탁했는데 지켜지지 않은 것"이고 A 는 같은 층위에 머문다. 그리고 로컬 스킬 29개 중 유일하게 이 키를 쓰던 것이 `xi-implementation`(가장 자주 쓰고 오발동 위험이 큰 스킬)이었다 — 이미 같은 선택을 한 전례다.

함께 고친 것 (2026-09-01) — `AGENTS.md` 의 "직접 언급하거나" 삭제, `## 필수 frontmatter` 절 추가, 권장 description 형태에서 bare-name 제거, 4개 스킬의 `description` 과 본문 활성화 조건에서 bare-name 제거.
